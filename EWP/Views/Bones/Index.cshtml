@model IEnumerable<EWP.Models.Bone>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoneName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoneName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoneID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoneID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoneID })
        </td>
    </tr>
}

</table>
